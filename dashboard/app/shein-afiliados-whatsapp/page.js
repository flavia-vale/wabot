import { PreservationCommercialPage, getPreservationCommercialMetadata } from '../_preservationCommercialPages'

export const metadata = getPreservationCommercialMetadata('shein-afiliados-whatsapp')

export default function Page() {
  return <PreservationCommercialPage pageKey="shein-afiliados-whatsapp" />
}
