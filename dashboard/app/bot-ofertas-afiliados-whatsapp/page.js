import { LpTemplate, getLpMetadata } from '../_lpShared'

export const metadata = getLpMetadata('bot-ofertas-afiliados-whatsapp')

export default function Page() {
  return <LpTemplate slug="bot-ofertas-afiliados-whatsapp" />
}
