import { PreservationCommercialPage, getPreservationCommercialMetadata } from '../_preservationCommercialPages'

export const metadata = getPreservationCommercialMetadata('magalu-afiliados-whatsapp')

export default function Page() {
  return <PreservationCommercialPage pageKey="magalu-afiliados-whatsapp" />
}
