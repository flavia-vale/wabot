import { PreservationCommercialPage, getPreservationCommercialMetadata } from '../_preservationCommercialPages'

export const metadata = getPreservationCommercialMetadata('amazon-afiliados-whatsapp')

export default function Page() {
  return <PreservationCommercialPage pageKey="amazon-afiliados-whatsapp" />
}
