import { LpTemplate, getLpMetadata } from '../_lpShared'

export const metadata = getLpMetadata('escalar-grupos-ofertas-sem-equipe')

export default function Page() {
  return <LpTemplate slug="escalar-grupos-ofertas-sem-equipe" />
}
