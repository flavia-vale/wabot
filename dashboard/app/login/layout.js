export const metadata = {
  title: 'Entrar ou Criar Conta',
  description: 'Acesse o painel do bot para converter links de afiliado, conectar o WhatsApp e automatizar envios de ofertas.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function LoginLayout({ children }) {
  return children;
}
