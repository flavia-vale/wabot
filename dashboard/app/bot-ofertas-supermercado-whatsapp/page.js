import { LpTemplate, getLpMetadata } from '../_lpShared'

export const metadata = getLpMetadata('bot-ofertas-supermercado-whatsapp')

export default function Page() {
  return <LpTemplate slug="bot-ofertas-supermercado-whatsapp" />
}
