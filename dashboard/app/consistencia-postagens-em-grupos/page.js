import { LpTemplate, getLpMetadata } from '../_lpShared'

export const metadata = getLpMetadata('consistencia-postagens-em-grupos')

export default function Page() {
  return <LpTemplate slug="consistencia-postagens-em-grupos" />
}
