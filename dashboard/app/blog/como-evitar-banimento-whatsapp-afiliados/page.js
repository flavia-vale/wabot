import { PreservationBlogPost, getPreservationBlogMetadata } from '../_preservationBlogPosts'

export const metadata = getPreservationBlogMetadata('como-evitar-banimento-whatsapp-afiliados')

export default function Page() {
  return <PreservationBlogPost postKey="como-evitar-banimento-whatsapp-afiliados" />
}
