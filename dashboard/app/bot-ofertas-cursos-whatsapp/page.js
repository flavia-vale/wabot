import { LpTemplate, getLpMetadata } from '../_lpShared'

export const metadata = getLpMetadata('bot-ofertas-cursos-whatsapp')

export default function Page() {
  return <LpTemplate slug="bot-ofertas-cursos-whatsapp" />
}
