import { LpTemplate, getLpMetadata } from '../_lpShared'

export const metadata = getLpMetadata('bot-ofertas-moda-whatsapp')

export default function Page() {
  return <LpTemplate slug="bot-ofertas-moda-whatsapp" />
}
