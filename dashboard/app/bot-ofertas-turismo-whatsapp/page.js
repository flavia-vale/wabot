import { LpTemplate, getLpMetadata } from '../_lpShared'

export const metadata = getLpMetadata('bot-ofertas-turismo-whatsapp')

export default function Page() {
  return <LpTemplate slug="bot-ofertas-turismo-whatsapp" />
}
