import { LpTemplate, getLpMetadata } from '../_lpShared'

export const metadata = getLpMetadata('espelhar-grupos-whatsapp-sao-paulo')

export default function Page() {
  return <LpTemplate slug="espelhar-grupos-whatsapp-sao-paulo" />
}
