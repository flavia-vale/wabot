import { LpTemplate, getLpMetadata } from '../_lpShared'

export const metadata = getLpMetadata('bot-ofertas-farmacia-whatsapp')

export default function Page() {
  return <LpTemplate slug="bot-ofertas-farmacia-whatsapp" />
}
