import { LpTemplate, getLpMetadata } from '../_lpShared'

export const metadata = getLpMetadata('bot-ofertas-beleza-whatsapp')

export default function Page() {
  return <LpTemplate slug="bot-ofertas-beleza-whatsapp" />
}
