import { LpTemplate, getLpMetadata } from '../_lpShared'

export const metadata = getLpMetadata('padronizar-divulgacao-afiliado-whatsapp')

export default function Page() {
  return <LpTemplate slug="padronizar-divulgacao-afiliado-whatsapp" />
}
