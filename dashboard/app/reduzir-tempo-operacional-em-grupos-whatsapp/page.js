import { LpTemplate, getLpMetadata } from '../_lpShared'

export const metadata = getLpMetadata('reduzir-tempo-operacional-em-grupos-whatsapp')

export default function Page() {
  return <LpTemplate slug="reduzir-tempo-operacional-em-grupos-whatsapp" />
}
