import { LpTemplate, getLpMetadata } from '../_lpShared'

export const metadata = getLpMetadata('bot-ofertas-autopecas-whatsapp')

export default function Page() {
  return <LpTemplate slug="bot-ofertas-autopecas-whatsapp" />
}
