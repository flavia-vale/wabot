import { LpTemplate, getLpMetadata } from '../_lpShared'

export const metadata = getLpMetadata('bot-ofertas-pet-shop-whatsapp')

export default function Page() {
  return <LpTemplate slug="bot-ofertas-pet-shop-whatsapp" />
}
