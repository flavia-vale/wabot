import { LpTemplate, getLpMetadata } from '../_lpShared'

export const metadata = getLpMetadata('automatizar-divulgacao-em-grupos-whatsapp')

export default function Page() {
  return <LpTemplate slug="automatizar-divulgacao-em-grupos-whatsapp" />
}
