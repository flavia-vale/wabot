import { PreservationCommercialPage, getPreservationCommercialMetadata } from '../_preservationCommercialPages'

export const metadata = getPreservationCommercialMetadata('bot-que-busca-ofertas-shopee-whatsapp')

export default function Page() {
  return <PreservationCommercialPage pageKey="bot-que-busca-ofertas-shopee-whatsapp" />
}
