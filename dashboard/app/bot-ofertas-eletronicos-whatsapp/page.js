import { LpTemplate, getLpMetadata } from '../_lpShared'

export const metadata = getLpMetadata('bot-ofertas-eletronicos-whatsapp')

export default function Page() {
  return <LpTemplate slug="bot-ofertas-eletronicos-whatsapp" />
}
